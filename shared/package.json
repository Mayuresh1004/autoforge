{
  "name": "@amass/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and utilities for AMASS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
